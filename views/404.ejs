<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>404 - Page Not Found | Friends Cart</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <style>
        :root {
            --primary: #4361ee;
            --secondary: #3a0ca3;
            --accent: #f72585;
            --background: #f8fafc;
            --text: #2d3748;
        }

        body {
            margin: 0;
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, var(--background), #e2e8f0);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: var(--text);
            overflow: hidden;
            perspective: 1500px;
            animation: pageLoad 1.2s ease-out;
        }

        @keyframes pageLoad {
            0% {
                opacity: 0;
                background-position: 0% 0%;
            }
            50% {
                opacity: 0.5;
                background-position: 50% 50%;
            }
            100% {
                opacity: 1;
                background-position: 100% 100%;
            }
        }

        .container {
            text-align: center;
            padding: clamp(1.5rem, 5vw, 3rem);
            background: rgba(255, 255, 255, 0.95);
            border-radius: clamp(15px, 3vw, 30px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(10px);
            transform-style: preserve-3d;
            animation: container-entrance 1.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            border: 2px solid rgba(255, 255, 255, 0.1);
            max-width: min(90%, 600px);
            width: 100%;
            position: relative;
            overflow: hidden;
        }

        @keyframes container-entrance {
            0% {
                opacity: 0;
                transform: 
                    perspective(1000px)
                    rotateX(30deg)
                    translateY(100px)
                    scale(0.7);
            }
            100% {
                opacity: 1;
                transform: 
                    perspective(1000px)
                    rotateX(0deg)
                    translateY(0)
                    scale(1);
            }
        }

        .container::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.8) 0%, rgba(255,255,255,0) 70%);
            animation: shine 8s infinite;
        }

        .logo {
            font-size: clamp(1.8rem, 7vw, 2.5rem);
            font-weight: 700;
            background: linear-gradient(135deg, var(--primary), var(--accent));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            margin-bottom: 1.5rem;
            position: relative;
            animation: logo-entrance 1.2s ease-out;
        }

        .logo::after {
            content: 'Friends Cart';
            position: absolute;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, transparent 45%, white 50%, transparent 55%);
            filter: blur(3px);
            animation: text-shine 3s infinite;
        }

        h1 {
            font-size: clamp(4rem, 15vw, 8rem);
            margin: 0;
            background: linear-gradient(135deg, var(--primary), var(--accent));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            animation: number-bounce 2s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            text-shadow: 2px 2px 15px rgba(0,0,0,0.1);
            letter-spacing: -2px;
        }

        @keyframes number-bounce {
            0% { transform: scale(0) rotate(-15deg); opacity: 0; }
            50% { transform: scale(1.2) rotate(5deg); }
            70% { transform: scale(0.9) rotate(-2deg); }
            100% { transform: scale(1) rotate(0deg); opacity: 1; }
        }

        h2 {
            font-size: clamp(1.2rem, 5vw, 2rem);
            margin: 0.5rem 0;
            animation: text-entrance 0.8s ease-out 0.5s both;
        }

        p {
            font-size: clamp(0.9rem, 4vw, 1.1rem);
            color: #64748b;
            margin-bottom: 1.5rem;
            line-height: 1.6;
            animation: text-entrance 0.8s ease-out 0.7s both;
        }

        .btn {
            display: inline-block;
            padding: clamp(0.8rem, 3vw, 1rem) clamp(1.5rem, 5vw, 2rem);
            font-size: clamp(0.9rem, 4vw, 1.1rem);
            font-weight: 600;
            color: white;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border: none;
            border-radius: 50px;
            text-decoration: none;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            animation: button-entrance 0.8s ease-out 0.9s both;
            box-shadow: 0 5px 15px rgba(67, 97, 238, 0.3);
        }

        .btn:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 
                0 10px 20px rgba(67, 97, 238, 0.4),
                0 6px 6px rgba(67, 97, 238, 0.3);
            animation: button-pulse 1s infinite;
        }

        @keyframes button-pulse {
            0% { box-shadow: 0 0 0 0 rgba(67, 97, 238, 0.4); }
            70% { box-shadow: 0 0 0 10px rgba(67, 97, 238, 0); }
            100% { box-shadow: 0 0 0 0 rgba(67, 97, 238, 0); }
        }

        @keyframes text-entrance {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes button-entrance {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes shine {
            0%, 100% {
                transform: rotate(0deg);
            }
            25% {
                transform: rotate(45deg);
            }
            50% {
                transform: rotate(90deg);
            }
            75% {
                transform: rotate(135deg);
            }
        }

        @keyframes text-shine {
            0% {
                transform: translateX(-100%);
            }
            100% {
                transform: translateX(100%);
            }
        }

        /* Floating animations for background elements */
        .bg-element {
            position: absolute;
            opacity: 0.1;
            animation: 
                float 6s ease-in-out infinite,
                spin 8s linear infinite;
            filter: blur(1px);
            z-index: -1;
        }

        @keyframes spin {
            from { transform: rotate(0deg) translateY(0); }
            to { transform: rotate(360deg) translateY(-20px); }
        }

        /* Landscape Mode Optimization */
        @media (max-height: 500px) and (orientation: landscape) {
            .container {
                padding: 1rem;
                margin: 1rem;
            }

            h1 { font-size: clamp(3rem, 12vw, 6rem); }
            p { margin-bottom: 1rem; }
            .logo { margin-bottom: 0.5rem; }
        }

        /* Mobile Responsiveness */
        @media (max-width: 768px) {
            .container {
                margin: 1rem;
                padding: 2rem;
            }

            h2 {
                font-size: clamp(1.2rem, 5vw, 2rem);
                margin: 0.5rem 0;
            }

            p {
                font-size: clamp(0.9rem, 4vw, 1.1rem);
                margin-bottom: 1.5rem;
            }

            .btn {
                padding: clamp(0.8rem, 3vw, 1rem) clamp(1.5rem, 5vw, 2rem);
                font-size: clamp(0.9rem, 4vw, 1.1rem);
            }

            .logo {
                font-size: clamp(1.8rem, 7vw, 2.5rem);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">Friends Cart</div>
        <h1 class="animate__animated animate__bounceIn">404</h1>
        <h2 class="animate__animated animate__fadeInUp">Oops! Page Not Found</h2>
        <p class="animate__animated animate__fadeInUp animate__delay-1s">
            The page you're looking for might have been removed, had its name changed, 
            or is temporarily unavailable.
        </p>
        <a href="/" class="btn animate__animated animate__fadeInUp animate__delay-1s">
            Return to Homepage
        </a>
    </div>

    <!-- Background Elements -->
    <script>
        function createBackgroundElements() {
            const shapes = ['⚪', '⬡', '⬢', '◇', '△'];
            const container = document.body;
            
            for (let i = 0; i < 15; i++) {
                const element = document.createElement('div');
                element.className = 'bg-element';
                element.style.left = Math.random() * 100 + 'vw';
                element.style.top = Math.random() * 100 + 'vh';
                element.style.fontSize = (Math.random() * 30 + 20) + 'px';
                element.style.animationDelay = Math.random() * 2 + 's';
                element.textContent = shapes[Math.floor(Math.random() * shapes.length)];
                container.appendChild(element);
            }
        }

        document.addEventListener('DOMContentLoaded', createBackgroundElements);
    </script>
</body>
</html>